import 'package:flutter/material.dart';

class TodoInsertPageLarge extends StatelessWidget {
  const TodoInsertPageLarge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ToDo - Insert - Large '),
    );
  }
}
