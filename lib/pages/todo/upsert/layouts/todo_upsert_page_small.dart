import 'package:flutter/material.dart';

class TodoUpsertPageSmall extends StatelessWidget {
  const TodoUpsertPageSmall({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
