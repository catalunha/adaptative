import 'package:flutter/material.dart';

class TodoUpdatePageLarge extends StatelessWidget {
  const TodoUpdatePageLarge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ToDo - Update - Large '),
    );
  }
}
